[build]
builder = "RAILPACK"
watchPatterns = ["/desale-backend/**", "/client/**"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
