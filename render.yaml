services:
  - type: web
    name: desale-backend
    runtime: node
    rootDir: desale-backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    plan: free
    envVars:
      - key: MONGODB_URI
        sync: false
